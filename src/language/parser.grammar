@top Program { statement* }

statement { Move | Turn | Repeat }

Move { "move" Number "steps" }

Turn { "turn" Direction }

Repeat { "repeat" Number "times" statement* "end" }

@tokens {
  Number { @digit+ }
  Direction { "left" | "right" }
  Comment { "#" ![\n]* }
  space { @whitespace+ }
  "move" "steps"
  "turn"
  "repeat" "times" "end"
}

@skip { space | Comment }

@detectDelim
